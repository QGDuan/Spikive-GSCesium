# Spikive GS Inspector（Windows x64）

本包适用于 64 位 Windows 10/11，已包含编译完成的前端和服务端，不包含示例模型或历史业务数据。

1. 先阅读 [USER_GUIDE.md](USER_GUIDE.md)。
2. 双击 `install.cmd`，等待依赖安装完成。
3. 双击 `start.cmd` 启动系统。
4. 使用 Chrome 或 Edge 打开 `http://localhost:3000`。

运行数据保存在本目录的 `var`，升级和迁移前请完整备份。系统生成的航线仅为候选结果，不能替代现场和飞行安全审核。
